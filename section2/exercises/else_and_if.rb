#assigning a value to a varaiable
people = 30
#assigning a value to a varaiable
cars = 40
#assigning a value to a varaiable
trucks = 15

#if boolean expression is true, it will run the code under it
if cars > people
  # prints statement in quotes
  puts "We should take the cars."
#if boolean expression is true, it will run the code under it
elsif cars < people
  #prints statement in quotes
  puts "we should not take the cars."
#if boolean expression is true, it will run the code under it
else
  #prints statement in quotes
  puts "We can't decide."
#ends if statement
end
#if boolean expression is true, it will run the code under it
if trucks > cars
  #prints statement in quotes
  puts "That's too many trucks."
#if boolean expression is true, it will run the code under it
elsif trucks < cars
  #prints statement in quotes
  puts "Maybe we could take the trucks."
#if boolean expression is true, it will run the code under it
else
  #prints statement in quotes
  puts "We still can't decide."
#ends if statement
end
#if boolean expression is true, it will run the code under it
if people > trucks
  #prints statement in quotes
  puts "Alright, let's just take the trucks."
#if boolean expression is true, it will run the code under it
else
  #prints statement in quotes
  puts "Fine, let's stay home then."
#ends if statment
end
#if boolean expression is true, it will run the code under it
if cars > people || trucks < cars
  #prints statement in quotes
  puts "What does this do?"
#ends if statement
end
#1 they are there to give different output depedening if boolean expression is true
