#Go through this program and write a comment above each line explaining it.
#completed

#Find all the places where a string is put inside a string.

#Are you sure there are only four places? How do you know? Maybe I like lying.
#Yes.  The last example concatenates two strings, it doesn't place them within each other.

#Explain why adding the two strings w and e with + makes a longer string.
#Concatenating strings combines the two strings into one string.

#What happens when you change the strings to use ' (single-quote) instead of " (double-quote)? Do they still work? Try to guess why.
#We receive a syntax error, unexpected local variable or method, expecting end-of-input do_not = 'don't' because we didn't use special characters around don't
